# Eino 框架智能客服系统架构

## 项目概述

这是一个基于 eino 框架的中等复杂度智能客服系统，展示了 eino 框架的三种编排方式：Chain、Graph 和 Workflow。

## 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            Eino 框架智能客服系统                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐            │
│  │   Chain 编排    │    │   Graph 编排    │    │  Workflow 编排  │            │
│  │  (简单链式)     │    │  (复杂有向图)   │    │  (高级编排)     │            │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘            │
│           │                       │                       │                    │
│           ▼                       ▼                       ▼                    │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐            │
│  │ 用户输入 → 消息  │    │ 用户输入 → 意图  │    │ 用户输入 → 数据  │            │
│  │ 构建 → 模型调用  │    │ 识别 → 分支处理  │    │ 预处理 → 意图分析 │            │
│  │ → 响应优化 → 输出│    │ → 上下文管理 →  │    │ → 上下文管理 →  │            │
│  └─────────────────┘    │ 知识检索 → 模型  │    │ 知识检索 → 消息  │            │
│                         │ 调用 → 响应优化  │    │ 构建 → 模型调用  │            │
│                         └─────────────────┘    │ → 响应优化 → 后  │            │
│                                                 │ 处理 → 输出      │            │
│                                                 └─────────────────┘            │
│                                                                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│                              Eino 框架组件层                                    │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │
│  │   ChatModel     │  │ IntentClassifier│  │ ContextManager  │  │KnowledgeRetr│ │
│  │   (聊天模型)    │  │   (意图分类器)  │  │  (上下文管理)   │  │(知识检索器) │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  └─────────────┘ │
│                                                                                 │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────┐ │
│  │ResponseOptimizer│  │   LambdaNode    │  │   BranchNode    │  │  StateNode  │ │
│  │  (响应优化器)   │  │   (Lambda节点)  │  │   (分支节点)    │  │  (状态节点) │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  └─────────────┘ │
│                                                                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│                              数据流转层                                         │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐            │
│  │   WorkflowData  │    │   SessionData   │    │   IntentData    │            │
│  │  (工作流数据)   │    │   (会话数据)    │    │   (意图数据)    │            │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘            │
│                                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐            │
│  │  KnowledgeData  │    │   MessageData   │    │  ResponseData   │            │
│  │  (知识数据)     │    │   (消息数据)    │    │  (响应数据)     │            │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘            │
│                                                                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│                              API 接口层                                         │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐            │
│  │  /api/v1/eino/  │    │  /api/v1/eino/  │    │  /api/v1/eino/  │            │
│  │  chain/invoke   │    │  graph/invoke   │    │ workflow/invoke │            │
│  │  chain/stream   │    │  graph/stream   │    │ workflow/stream │            │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘            │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

## 三种编排方式对比

### 1. Chain 编排（简单链式流程）

**特点：**
- 线性执行，数据单向流动
- 适合简单的问答对话
- 执行效率高，延迟低

**流程：**
```
用户输入 → 消息构建 → 模型调用 → 响应优化 → 输出
```

**适用场景：**
- 简单问答
- 创意写作
- 快速原型开发

### 2. Graph 编排（复杂有向图流程）

**特点：**
- 支持分支、循环和条件判断
- 支持意图识别和智能路由
- 适合复杂的业务逻辑

**流程：**
```
用户输入 → 意图识别 → 分支选择 → 上下文管理 → 知识检索 → 模型调用 → 响应优化 → 输出
                ↓
        [问候/问题/投诉/转人工/创作/默认]
```

**适用场景：**
- 智能客服
- 意图识别
- 条件分支处理

### 3. Workflow 编排（高级编排流程）

**特点：**
- 支持复杂的数据流转和字段级映射
- 支持状态管理和元数据跟踪
- 适合企业级应用

**流程：**
```
数据输入 → 预处理 → 意图分析 → 上下文管理 → 知识检索 → 消息构建 → 模型调用 → 响应优化 → 后处理 → 输出
    ↓         ↓         ↓          ↓          ↓          ↓          ↓          ↓         ↓
  用户输入   清理数据   关键词匹配   构建上下文   构建消息   模型调用   格式化输出   元数据管理
```

**适用场景：**
- 企业级客服系统
- 复杂工作流
- 高可观测性要求

## Eino 框架核心功能

### 1. 组件化设计
- **ChatModel**: 聊天模型组件，支持同步和流式调用
- **IntentClassifier**: 意图分类器，支持多种意图识别
- **ContextManager**: 上下文管理器，维护会话状态
- **KnowledgeRetriever**: 知识检索器，支持知识库查询
- **ResponseOptimizer**: 响应优化器，优化输出质量

### 2. 编排能力
- **Chain**: 简单链式编排，适合线性流程
- **Graph**: 复杂图编排，支持分支和循环
- **Workflow**: 高级工作流编排，支持字段级映射

### 3. 数据流转
- **字段级映射**: 支持复杂数据结构在节点间传递
- **状态管理**: 全局状态管理，确保数据一致性
- **元数据跟踪**: 完整的执行日志和监控

### 4. 流式处理
- **自动拼接**: 自动处理流式数据的拼接、合并
- **实时响应**: 支持实时流式响应
- **错误处理**: 完善的错误处理和恢复机制

## 技术实现亮点

### 1. 真正的 Eino 框架集成
- 使用 eino 框架的组件化设计理念
- 实现标准的 Invoke 和 Stream 接口
- 支持复杂的数据流转和字段级映射

### 2. 中等复杂度设计
- 智能客服系统，包含意图识别、知识检索、上下文管理
- 三种不同复杂度的编排方式
- 完整的错误处理和日志记录

### 3. 生产就绪
- 真实的 GLM4.5 API 集成
- 完整的 HTTP 接口
- 详细的监控和日志

### 4. 可扩展性
- 模块化设计，易于扩展
- 支持自定义组件
- 支持自定义编排逻辑

## 使用示例

### Chain 编排示例
```bash
# 简单问答
curl -X POST http://localhost:8888/api/v1/eino/chain/invoke \
  -H "Content-Type: application/json" \
  -d '{"message": "你好，请介绍一下自己"}'
```

### Graph 编排示例
```bash
# 意图识别和分支处理
curl -X POST http://localhost:8888/api/v1/eino/graph/invoke \
  -H "Content-Type: application/json" \
  -d '{"message": "什么是人工智能？"}'
```

### Workflow 编排示例
```bash
# 复杂工作流处理
curl -X POST http://localhost:8888/api/v1/eino/workflow/invoke \
  -H "Content-Type: application/json" \
  -d '{"message": "你好，请介绍一下自己"}'
```

## 总结

这个项目真正展示了 eino 框架的核心能力：

1. **组件化设计**: 通过可复用的组件构建复杂应用
2. **编排能力**: 支持三种不同复杂度的编排方式
3. **数据流转**: 支持复杂数据在节点间的流转和映射
4. **流式处理**: 自动处理流式数据的拼接和合并
5. **状态管理**: 全局状态管理和会话维护
6. **可观测性**: 完整的日志记录和监控

这是一个真正基于 eino 框架的中等复杂度项目，展示了框架的核心功能和最佳实践。
